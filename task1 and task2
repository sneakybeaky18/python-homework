# 1. Создать программно файл в текстовом формате,
# записать в него построчно данные, вводимые пользователем.
# Об окончании ввода данных свидетельствует пустая строка.

datalist = []
while True:
    data = input("Введите данные. Чтобы завершить ввод, нажмите ENTER: \n")
    datalisttransform = data
    datalist.append(datalisttransform)
    if data == "":
        break
listToStr = '\n'.join([str(elem) for elem in datalist])
print(listToStr)

with open("data2.txt", "w") as file:
    file.writelines(listToStr)
    file.close()

# 2. Создать текстовый файл (не программно),
# сохранить в нем несколько строк, выполнить подсчет
# количества строк, количества слов в каждой строке.

wordsFile = open("data2.txt", "r", encoding="utf-8")
linesFile = open("data2.txt", "r", encoding="utf-8")

readed = wordsFile.read()
imported_Lines = linesFile.readlines()
lines = len(imported_Lines)
splited_r = readed.split()
words_count = len(splited_r)
print(f"Количество линий в документе: {lines}\nКоличество слов в документе: {words_count}")
