# 6. Необходимо создать (не программно) текстовый файл,
# где каждая строка описывает учебный предмет и наличие лекционных,
# практических и лабораторных занятий по этому предмету и их количество.
# Важно, чтобы для каждого предмета не обязательно были все типы занятий.
# Сформировать словарь, содержащий название предмета и общее количество занятий по нему. Вывести словарь на экран.
# Примеры строк файла:
# Информатика: 100(л) 50(пр) 20(лаб).
# Физика: 30(л) — 10(лаб)
# Физкультура: — 30(пр) —
#
# Пример словаря:
# {“Информатика”: 170, “Физика”: 40, “Физкультура”: 30}


ziped = open("code.txt", "r", encoding="utf-8")
readed = ziped.readlines()

dict = dict(subject="subject", hours="digit")
listOfSubjects = []
totalHours = []
for el in readed:
    subject, points, subjectiteration, digit2, subjectiteration2 = el.split()
    inted = int(points)
    inted2 = int(digit2)
    totalhours = inted+inted2
    listOfSubjects.append(subject)
    listOfSubjects.append(totalhours)

dictOfWords = dict.fromkeys(listOfSubjects, ":")
print(dictOfWords)
